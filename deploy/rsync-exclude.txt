# rsync exclude list for deploying GNRS to the remote host.
# Everything here is either a secret, a local artifact, or rebuilt
# inside the container image — none of it belongs in the build context.

# Version control
.git/

# Secrets / local env — the remote keeps its own .env (see deploy.sh)
.env
.env.local
.env.*.local

# Dependencies — reinstalled by the image build (npm ci / go mod download)
node_modules/
web/app/node_modules/
.pnpm-store/

# Build outputs — regenerated inside the image build
web/app/dist/
web/dist/
/server
*.exe
*.test
*.out
*.tsbuildinfo
import-historical
import-historical-linux
backfill-pencapaian
backfill-pencapaian-linux

# Generated route tree — regenerated by the SPA build
web/app/src/routeTree.gen.ts

# Local data / SQLite
data/
*.db
*.db-journal
*.db-shm
*.db-wal

# CSV / Excel / analysis artifacts
*.csv
excel/
nickname_mapping.md
kehadiran.jpeg

# IDE / OS / agent state
.idea/
.vscode/
.claude/
.seed/
.DS_Store
Thumbs.db
*.swp
*.swo
